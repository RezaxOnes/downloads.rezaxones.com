const user = "rezaxonesassist";
const domain = "gmail.com";
const container = document.getElementById("My_Email");
container.innerHTML = `<a href="mailto:${user}@${domain}" style="color:#000000;">${user}@${domain}</a>`;
